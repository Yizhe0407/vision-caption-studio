services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_started
      minio:
        condition: service_started
    env_file:
      - .env
    environment:
      NODE_ENV: production
      DATABASE_URL: mysql://root:password@mysql:3306/vision_caption_studio
      REDIS_HOST: redis
      MINIO_ENDPOINT: minio
      MINIO_USE_SSL: "false"
    command: >
      sh -c "until nc -z mysql 3306; do sleep 1; done;
      pnpm prisma generate;
      pnpm prisma db push;
      pnpm db:seed;
      pnpm exec next start --hostname 0.0.0.0 --port 3000"
    restart: unless-stopped
    ports:
      - "3000:3000"

  worker:
    build:
      context: .
      dockerfile: Dockerfile
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_started
      minio:
        condition: service_started
    env_file:
      - .env
    environment:
      NODE_ENV: production
      DATABASE_URL: mysql://root:password@mysql:3306/vision_caption_studio
      REDIS_HOST: redis
      MINIO_ENDPOINT: minio
      MINIO_USE_SSL: "false"
    command: >
      sh -c "until nc -z mysql 3306; do sleep 1; done;
      pnpm prisma generate;
      pnpm prisma db push;
      pnpm db:seed;
      pnpm worker"
    restart: unless-stopped

  mysql:
    image: mysql:8.4
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: vision_caption_studio
    restart: unless-stopped
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "127.0.0.1", "-ppassword"]
      interval: 5s
      timeout: 3s
      retries: 20

  redis:
    image: redis:7.4-alpine
    restart: unless-stopped
    ports:
      - "6379:6379"

  minio:
    image: minio/minio:latest
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    restart: unless-stopped
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio_data:/data

volumes:
  mysql_data:
  minio_data:
